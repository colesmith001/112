# steps to create a django project 

1.create thr folder

2.open vs code and terminal

3.create a virtual environment (venv) python -m venv venv// mac python3 

4.activate venv // .\venv\Scripts\activate

5. install django // pip install django

6. start django project//  django-admin startproject config(folder name) ."# 112" 
